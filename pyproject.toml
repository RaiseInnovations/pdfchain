[tool.poetry]
name = "pdfchain"
version = "0.1.0"
description = "Let LLMs understand pdf files easily"
authors = ["Raise Lab"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
